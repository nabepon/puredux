import assert from 'power-assert';
import { recurseObject } from '../src/utils';

describe('utils', () => {
  describe('recurseObject', () => {
    it('test', () => {
      assert(1 === 1);
    });
  });
});
